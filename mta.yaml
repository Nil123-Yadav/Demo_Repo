ID: python-hello
_schema-version: '3.1'
version: 0.0.1

modules:
  - name: ${APP_NAME}
    type: application
    path: .
    parameters:
      memory: ${MEMORY}
      disk-quota: 512M
      buildpack: python_buildpack
      instances: 1
