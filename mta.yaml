ID: python-hello
_schema-version: '3.1'
version: 0.0.1

modules:
  - name: python-app
    type: python.app
    path: .
    build-parameters:
      builder: custom
      supported-platforms: []
      commands:
        - echo "No build step required for Python app"
